__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
_train_work/
validator_state.db
*.db
.DS_Store

# Secrets — never commit
.env
*.env
!*.env.example

# Local-only config overrides (carry live eval-pool refs / secrets) and pool caches.
# chain.testnet.toml IS tracked; the eval-pool ref is set out-of-band, not committed.
chain.*.local.toml
_pool_real/
_eval_pool/
_vpool_cache/

# Trainer-local remote-host file (pod IPs / key paths) — keep off git/chain.
hosts.toml
# Service logs
orchestrator.log
validator.log
worker_manual.log

# live per-box provisioner config (copy from deploy/provision.*.toml)
/provision.toml

# local proposal prompts (not for the repo)
/PROPOSAL-*.md
receipts_cache/

# Local operator deploy runbook — supplied to the install agent out-of-band, never committed
docs/DEPLOY_FUNDED_MAINNET.md
wandb/

models/
generators/
cascade-eval-pool/
train-pool/
api/
